# The frontend currently uses relative fetch URLs and works with the backend on the same origin.
# Keep this here only if you later move the frontend to a different host.
API_BASE_URL=http://localhost:3000
